export const done_to_open = (item) =>({
    type:"DONE_TO_OPEN",
    item
});

export const done_to_inprocess = (item) =>({
    type:"DONE_TO_INPROCESS",
    item
});

export const done_to_review = (item) =>({
    type:"DONE_TO_REVIEW",
    item
});
